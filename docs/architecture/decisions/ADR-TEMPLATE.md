# ADR-TEMPLATE

> **Document Info**
> - Mục đích: khuôn mẫu cho mọi ADR mới — copy file này, đổi tên theo
>   quy ước `MODULE-Dn-slug.md`, điền nội dung.
> - Đối tượng đọc chính: người viết ADR (ChatGPT), người review ADR
>   (Claude).
> - Trạng thái: **Template** — không tự nó là một quyết định, không
>   trích dẫn file này như một ADR thật.
> - Tài liệu liên quan: [README.md](README.md) (quy ước lifecycle/naming).

---

## `<MODULE>-D<n>`: `<Tên quyết định ngắn gọn>`

**Status:** Proposed | Accepted | Superseded by `<ID>` | Deprecated

**Ngày:** YYYY-MM-DD

**Bối cảnh (Context)**

Vấn đề gì cần quyết định? Vì sao nó phát sinh? Nếu có bằng chứng cụ thể
theo Burden of Proof Rule (thất bại/hạn chế của implementation hiện
tại), nêu rõ ở đây.

**Quyết định (Decision)**

Nội dung quyết định — bằng tiếng Việt (có thể copy trực tiếp vào Decision
Log dự án).

**Lý do (Reason)**

Vì sao chọn phương án này, không chọn phương án khác. Nếu có phương án
bị loại bỏ, nêu ngắn gọn lý do loại bỏ.

**Hệ quả (Consequences)**

Ảnh hưởng cụ thể tới code/tài liệu/module khác nếu quyết định này được
áp dụng.

**Liên quan (Related)**

- ADR liên quan: `<ID>`
- Tài liệu frozen liên quan: `<link>`
